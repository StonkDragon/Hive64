main:
    ldr r0 1 ; syscall 1 (write)
    ldr r1 1 ; stdout
    ldr r2 msg ; address of message (must be 0 terminated)
    ldr r3 14 ; length of message
    svc ; syscall

    ldr r0 0 ; syscall 0 (exit)
    ret

msg: .asciz "Hello, world!\n"
