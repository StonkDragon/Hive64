.text
.global _main
_main:
    str lr, [sp, -16]!
    
    ldrb r0, [msg]
    byte dec r0
    strb r0, [msg]
    
    lea r0, msg
    bl _puts

    xor r0, r0

    ldr lr, [sp, 16]!
    ret

.bss
msg:
    .asciz "Hello, World!"
